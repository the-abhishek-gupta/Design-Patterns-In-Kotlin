package org.example.structural

import org.example.structural.adapter.AdapterMain
import org.example.structural.bridge.BridgeMain
import org.example.structural.composite.MainComposite
import org.example.structural.decorator.MainDecorator
import org.example.structural.facade.FacadeMain
import org.example.structural.flyweight.FlyweightMain
import org.example.structural.proxy.ProxyMain

class StructuralMain {
    fun main(){
//        AdapterMain().main()
//        BridgeMain().main()
//        MainComposite().main()
//        MainDecorator().main()
//        FacadeMain().main()
//        ProxyMain().main()
        FlyweightMain().main()
    }
}